pipeline{
    agent none
    stages{
        stage('build'){
            dockerfile {
                label 'build-slave'
            }
            steps{
                sh 'node --version'
            } 
         }
    }
}
