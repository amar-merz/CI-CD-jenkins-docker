pipeline{
    agent none
    stages{
        stage('build'){
            agent {
                dockerfile{
                label 'build-slave'
                dir 'nodejsapp'
                additionalBuildArgs '--tag adam/nodejsimg'
                }
            }
            steps{
                sh 'node --version'
            } 
         }
    }
}
