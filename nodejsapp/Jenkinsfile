pipeline{
    agent none
    stages{
        stage('build'){
            agent {
                dockerfile{
                image 'adam/nodejsimg'
                label 'build-slave'
                dir 'nodejsapp'
                }
            }
            steps{
                sh 'node --version'
            } 
         }
    }
}
