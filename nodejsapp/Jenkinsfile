pipeline{
    agent none
    stages{
        stage('build'){
            agent {
                dockerfile{
                label 'build-slave'
                }
            }
            steps{
                sh 'node --version'
            } 
         }
    }
}
