pipeline{
    agent none
    stages{
        stage('build'){
            agent {
                dockerfile{
                label 'build-slave'
                dir 'nodejsapp'
                args '-t adam/nodejsimg'
                }
            }
            steps{
                sh 'node --version'
            } 
         }
    }
}
