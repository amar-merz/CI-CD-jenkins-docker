pipeline{
    agent none
    stages{
        stage('build'){
            agent {
                dockerfile{
                label 'build-slave'
                dir 'nodejsapp'
                additionalBuildArgs '-t adam/nodejsimg'
                }
            }
            steps{
                sh 'node --version'
            } 
         }
    }
}
